// import greeting from './components/greeting';
// import intro from './components/intro';
// import rules from './components/rules';

const ELEMENTS = {
  main: document.querySelector(`#main`),
  // introasterisk: intro.document.querySelector(`.intro__asterisk`),
  // greetingcontinue: greeting.document.querySelector(`.`),
  // rulesinput: rules.document.querySelector(`.rules__input`),
  // rulesbutton: rules.document.querySelector(`.rules__button`),
};

export default ELEMENTS;
